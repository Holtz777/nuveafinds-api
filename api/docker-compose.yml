services:
  api:
    build: .
    container_name: nuveafinds-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    # Sem volumes: imagem é autocontida.
